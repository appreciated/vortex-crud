# SQLite Datasource
spring.application.name=sqlite
spring.datasource.url=jdbc:sqlite:jooq-example-database.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# Liquibase
spring.liquibase.enabled=true