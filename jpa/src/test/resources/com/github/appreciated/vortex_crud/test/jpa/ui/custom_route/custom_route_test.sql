-- CustomRoute test - no database setup needed
-- CustomRoute simply links to a @Route annotated view, no entities required

-- No-op statement to ensure the script has executable SQL for CI environments
SELECT 1;
